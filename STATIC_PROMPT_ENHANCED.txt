# SLIDE CONTENT MODIFICATION TASK

## OBJECTIVE
Modify ONLY the text content in the uploaded/provided PptxGenJS JavaScript code based on the new content information from our previous discussion, attached documents, or provided in this message.

## ⚠️ CRITICAL OUTPUT REQUIREMENT ⚠️

**YOU MUST RETURN THE COMPLETE, COPY-PASTE-READY JAVASCRIPT CODE**

NOT just the changed elements - the ENTIRE file from start to finish!

The user should be able to:
1. Copy your ENTIRE output
2. Paste into a new .js file
3. Run: node filename.js
4. Get a working .pptx file

**Example of WRONG output (DO NOT do this):**
```javascript
// Element 2 - MODIFIED
slide.addText("New Title", { ... });

// Element 7 - MODIFIED
slide.addText("New description", { ... });
```
❌ This is incomplete - user cannot run it!

**Example of CORRECT output (DO this):**
```javascript
// Generated code for Slide 4 from filename.pptx
const pptxgen = require("pptxgenjs");

let pres = new pptxgen();
pres.layout = 'LAYOUT_16x9';
pres.author = 'Generated by PPT2Code';
pres.title = 'Converted Presentation';

let slide = pres.addSlide();

slide.background = { color: "F0F4F8" };

// Element 1 - UNCHANGED
slide.addShape(pres.shapes.RECTANGLE, {
  x: 0.00, y: 0.00,
  w: 10.00, h: 0.06,
  fill: { color: "00B4D8" }
});

// Element 2 - MODIFIED
// Original: "Old Title" (2 words)
// New: "New Title" (2 words) ✓ Within ±20% limit
slide.addText("New Title", {
  x: 0.70, y: 0.30,
  w: 8.00, h: 0.60,
  fontSize: 28.0,
  fontFace: "Georgia",
  color: "1A2332",
  bold: true,
  align: "left"
});

// [... ALL OTHER ELEMENTS ...]

// Element 38 - UNCHANGED
slide.addImage({
  path: "images/filename_slide4_img6.png",
  x: 8.95, y: 3.28,
  w: 0.28, h: 0.28
});

pres.writeFile({ fileName: "output_filename.pptx" });
console.log("✅ Slide recreated successfully!");
```
✅ This is complete - user can run it immediately!

---

## CRITICAL RULES - NEVER VIOLATE

### PRESERVE ALL FORMATTING (DO NOT CHANGE)
- ❌ DO NOT modify x, y coordinates (positioning)
- ❌ DO NOT change w, h dimensions (sizing)
- ❌ DO NOT alter fontSize values
- ❌ DO NOT change fontFace values
- ❌ DO NOT modify color values (hex codes)
- ❌ DO NOT change alignment (align, valign)
- ❌ DO NOT alter styling (bold, italic)
- ❌ DO NOT modify shapes, backgrounds, images
- ❌ DO NOT add or remove elements
- ❌ DO NOT reorder elements

### CONTENT MODIFICATION RULES
- ✅ Replace text ONLY in slide.addText() calls
- ✅ Keep word count within ±20% for EACH text element
- ✅ Maintain similar sentence structure and brevity
- ✅ Preserve tone and style from original
- ✅ Keep same number of text elements

### WORD COUNT ENFORCEMENT
For EACH modified text element:
1. Count words in original text
2. Calculate allowed range: [original × 0.8] to [original × 1.2]
3. Ensure replacement fits within range
4. If too long → Make concise, remove filler
5. If too short → Add relevant context

---

## INPUT SOURCES (Automatic Detection)

**Original Code:** 
- Uploaded .js file
- Uploaded .txt file containing code
- Code from previous messages
- Code provided in this message

**New Content Information:**
- Previous chat discussion/context
- Attached documents (PDF, DOCX, TXT)
- New information in this message
- Summarized content from earlier conversation

---

## COMPLETE OUTPUT STRUCTURE (MANDATORY)

Your output MUST include ALL of these sections in order:

```javascript
// 1. HEADER COMMENT
// Generated code for Slide X from filename.pptx
// IMPORTANT: This code expects an 'images/' folder in the same directory

// 2. PPTXGEN SETUP (REQUIRED)
const pptxgen = require("pptxgenjs");

let pres = new pptxgen();
pres.layout = 'LAYOUT_16x9';
pres.author = 'Generated by PPT2Code';
pres.title = 'Converted Presentation';

let slide = pres.addSlide();

// 3. BACKGROUND (if present in original)
slide.background = { color: "XXXXXX" };

// 4. ALL SLIDE ELEMENTS (EVERY SINGLE ONE)
// Include EVERY element from the original code
// Mark modified elements with comments, leave others as-is

// Element 1 - UNCHANGED
slide.addShape(...);

// Element 2 - MODIFIED
// Original: "text" (N words)
// New: "text" (M words) ✓ Within ±20% limit
slide.addText(...);

// Element 3 - UNCHANGED
slide.addShape(...);

// [Continue for ALL elements - don't skip any!]

// 5. SAVE COMMAND (REQUIRED)
pres.writeFile({ fileName: "output_filename.pptx" });

// 6. CONSOLE OUTPUT (REQUIRED)
console.log("✅ Slide recreated successfully!");

// 7. IMAGE LIST (if images present)
console.log("📋 Required images (place in images/ folder):");
console.log("   - images/filename_slide4_img1.png");
// [List all images]
```

**CRITICAL:** Do not truncate, summarize, or skip sections. Return the COMPLETE file!

---

## ELEMENT COMMENTING RULES

**For UNCHANGED elements:**
```javascript
// Element 5 - UNCHANGED
slide.addText("01", {
  x: 0.70, y: 1.15,
  w: 0.55, h: 0.40,
  fontSize: 13.0,
  fontFace: "Calibri",
  color: "FFFFFF",
  bold: true,
  align: "center"
});
```

**For MODIFIED elements:**
```javascript
// Element 6 - MODIFIED
// Original: "Initiate" (1 word)
// New: "Application" (1 word) ✓ Within ±20% limit
slide.addText("Application", {
  x: 0.90, y: 1.65,
  w: 2.40, h: 0.35,
  fontSize: 14.0,
  fontFace: "Georgia",
  color: "1A2332",
  bold: true,
  align: "left"
});
```

**For shapes and images:**
```javascript
// Element 3 - UNCHANGED
slide.addShape(pres.shapes.RECTANGLE, {
  x: 0.70, y: 1.15,
  w: 2.80, h: 1.80,
  fill: { color: "FFFFFF" }
});

// Element 33 - UNCHANGED
slide.addImage({
  path: "images/filename_slide4_img1.png",
  x: 2.85, y: 1.23,
  w: 0.28, h: 0.28
});
```

---

## PRE-SUBMISSION CHECKLIST

Before returning your output, verify:

**Structure Completeness:**
- [ ] Starts with: `const pptxgen = require("pptxgenjs");`
- [ ] Includes: `let pres = new pptxgen();`
- [ ] Includes: `pres.layout = 'LAYOUT_16x9';`
- [ ] Includes: `let slide = pres.addSlide();`
- [ ] Includes background (if in original)
- [ ] Includes ALL elements from original
- [ ] Ends with: `pres.writeFile({ fileName: "..." });`
- [ ] Ends with: console.log statements

**Element Completeness:**
- [ ] Same total number of elements as original
- [ ] All shapes included
- [ ] All text boxes included
- [ ] All images included
- [ ] Elements in same order

**Formatting Preservation:**
- [ ] All x, y values unchanged
- [ ] All w, h values unchanged
- [ ] All fontSize values unchanged
- [ ] All fontFace values unchanged
- [ ] All color values unchanged
- [ ] All alignment values unchanged

**Content Validation:**
- [ ] Only text content modified
- [ ] Word count within ±20% for each modified element
- [ ] Word count comments present for modified elements

**Output Readiness:**
- [ ] Code is syntactically correct JavaScript
- [ ] Can be copied and pasted as-is
- [ ] Can be run immediately with: node filename.js
- [ ] Will generate a .pptx file when run

---

## ERROR HANDLING

If content cannot fit within ±20% word limit:
- Add comment: `// WARNING: Word limit exceeded - review needed`
- Provide best-fit content anyway
- Do NOT change formatting to make it fit
- Still return COMPLETE code with all elements

If you encounter any issues:
- Still return the COMPLETE code
- Add warning comments where needed
- Never return partial or summarized code

---

## FINAL REMINDER

**DO:**
✅ Return COMPLETE, RUNNABLE code
✅ Include ALL elements (modified and unchanged)
✅ Include all setup and teardown code
✅ Add word count comments for modified elements
✅ Preserve exact formatting for all elements

**DO NOT:**
❌ Return only changed elements
❌ Summarize or truncate the code
❌ Skip unchanged elements
❌ Omit setup or save commands
❌ Change any formatting properties

---

## PROCEED

Analyze the provided code and new content, then return the COMPLETE, READY-TO-RUN modified JavaScript code.

**The user should be able to copy your entire response, save it as a .js file, and run it immediately without any modifications.**
